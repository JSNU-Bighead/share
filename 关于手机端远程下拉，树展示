手机端相较于PC端的差异，使得pc端下拉，树形结构的交互不适用。
开始决定在本页面全屏的弹层来实现，但是对于touchmove事件问题和z-index问题（可能有好的解决方案吧，只是我这边没有找到比较好的）。
最后决定这样的场景采用 新路由页 + vuex 来实现。
在vuex的state中单独一个模块，存放下拉数据源。
将获取方法mapActions 到新路由页（组件）中然后 store.diapatch('...')
需要使用下拉的地方路由跳转，带参（actions 的类型，选用路由带参是因为，参数不会很长，然后如果存放$store或者全局变量，在页面刷新后参数丢失）
